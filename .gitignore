# Python
__pycache__/
*.py[cod]

# Django secrets — each machine has its own copy, never committed
isfdb_site/local_settings.py

# Virtual environment
venv/
.venv/

# Large data files not needed in the repo
isfdb_schema.sql

# Original source images (processed copies live in magazine/static/)
homeimages/

# Claude Code local config
.claude/settings.local.json
